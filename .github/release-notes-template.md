# Osu! Panel v{{VERSION}}

Released from pull request {{PR}} — {{PR_TITLE}}.

> This release was created automatically from a `/release` comment on the pull request.
> It is a draft — review the notes and publish it when ready.

## APKs

- `app-debug.apk` — always attached, installable without signing.
- `app-release.apk` — attached only when the `SIGN_*` repository secrets are configured; otherwise the release APK is unsigned.
